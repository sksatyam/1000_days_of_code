package OOPS;

public class MethodOverloading {
    // two or more methods can have same name inside a same class if they accepts different arguments. this featre is knows as method overloading.

    // Method overloading is achieved by either:
    //    1) Changing the number of arguments
    //     2) or changing te datatype of the arguments 
    
    // It is not methodoverloading if we only change the return type of methods.
    //    There must be differences in the nmber of parameters.
}
