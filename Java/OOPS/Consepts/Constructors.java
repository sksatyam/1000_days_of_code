package OOPS.Consepts;

public class Constructors {
    
    // Constructors are invoked implicitly when we instantiate objects.
    
    /*
     The two rules of creating constructors are:
        1.  The name of  the constructor shuld be same as the class
        2.  A java constructor mut not have a return type. 
    */

    // If a class doesn't have a contructor, the java conpiler automatically creates a default constructor during run-time. the default constructor initializes instance variables with edfault values.

    // Aconsructor cannot be abstract or static or final.

    // A constructor can be overloaded but connot be be overridden.

}
