package Quick_Reminders;

public class Constructor2 {
    // If we create a class with private constructor and if we have public static methods in that class then in that case we can use those methods in static methods without creating its objects
    
    // We cant inherit from a class having private constructor because when we inherite from a class the child class implicity class supers classes constructor and in this case constructor of super class is private means it cant be accessed outside the class
}
