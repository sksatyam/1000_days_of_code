// package Quick_Reminders;

public class constructor1 {
    // by default acces modefier of constructor is default and it also knows as
    // package-private which means it can only be initaliezed within the same
    // package other wire it will throw an error
    // Another type is private this means this class can't be initalized
    // and last one is public access modiefier this means it can be initalize even
    // outside the package
}
